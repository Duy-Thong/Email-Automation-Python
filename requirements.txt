# Email Automation Tool - Dependencies
# Core GUI and image processing
customtkinter>=5.2.0
Pillow>=10.0.0

# Excel file handling
openpyxl>=3.1.0

# Standard library modules (already included in Python):
# - smtplib (email sending)
# - email.mime.multipart, email.mime.text (email formatting)
# - tkinter (GUI - included with Python)
# - webbrowser (open URLs)
# - os, sys (system operations)
# - time (delays)
# - concurrent.futures (threading)
